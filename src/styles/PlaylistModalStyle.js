import { StyleSheet } from 'react-native';

const PlaylistModalStyle = StyleSheet.create({
  modalContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#392F2F',
    padding: 20,
    marginHorizontal: '10%',
    height: '40%',
    borderRadius: 10,
  },
});

export default PlaylistModalStyle;